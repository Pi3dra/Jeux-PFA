open Component_defs
open System_defs

let create id x y w h color mass =  failwith "Todo"

  (*
  let e = new block() in
  e#box#set Rect.{width = w ; height = h};
  e#mass#set mass;
  e#kuchao#set Vector.zero;
  e#sum_forces#set Vector.zero;
  e#id#set id;

  Collision_system.(register (e :> t));
  Move_system.(register (e :> t));

  e*)

 